# URL pública sin barra final. En local no hace falta.
NEXT_PUBLIC_SITE_URL=https://example.com

# Solo "true" en el build de PRODUCCIÓN con dominio propio.
# En previews (*.pages.dev) debe quedar vacío: robots.txt bloquea y todo va con noindex.
NEXT_PUBLIC_INDEXABLE=
