# Public URL without trailing slash. Not needed locally.
NEXT_PUBLIC_SITE_URL=https://example.com

# Only "true" in the PRODUCTION build on the real domain.
# Leave it empty on previews (*.pages.dev): robots.txt blocks and every page is noindex.
NEXT_PUBLIC_INDEXABLE=
